## TEST AGENT PROMPT – IMAGE INTEGRATION RULES

Always use base64-encoded images for tests.
Accepted: JPEG, PNG, WEBP only. NEVER SVG/BMP/HEIC.
Don't use blank/solid-color images.
Every image must have real features.
For animated GIF/WEBP, extract first frame only.
Resize large images.
